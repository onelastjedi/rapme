version: 2
jobs:
  build:
    working_directory: ~/rapme
    docker:
      - image: circleci/node:8.1.2-browsers:latest
    steps:
      - checkout
      - run:
          name: install-npm-wee
          command: npm install
      - run:
          name: build
          command: npm build
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - .node_modules
  test:
    working_directory: ~/rapme
    docker:
      # - image: selenium/standalone-chrome:3.1.0
      - image: circleci/node:8.1.2-browsers:latest
    steps:
      - run:
          name: test
          command: node test/e2e/runner.js
      - run:
          name: code-coverage
          command: './node_modules/.bin/nyc report --reporter=text-lcov'
      - store_artifacts:
          path: test-results.xml
          prefix: tests
      - store_artifacts:
          path: coverage
          prefix: coverage
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
          filters:
            branches:
              only: master
